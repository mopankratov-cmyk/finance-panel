<!-- Заполни — это уйдёт владельцу в Telegram и ускорит проверку. -->

## Что сделал
<!-- 1–3 предложения: какую функцию добавил / что починил -->

## Зачем
<!-- какую задачу решает -->

## Как проверить
<!-- шаги: что открыть, что нажать, что должно произойти -->

## Новые переменные окружения
<!-- Если фиче нужна новая env-переменная: укажи ИМЯ + что это + где взять значение.
     Имя с заглушкой добавь в .env.example. Владелец заведёт значение в Vercel. Иначе — «нет». -->
нет

## Чек-лист
- [ ] Запускал локально (`npm run dev`), не падает
- [ ] Не вставлял **реальные** секреты/ключи в код и в `.env.example` (только имена с заглушками)
- [ ] Новые env-переменные (если есть) добавил в `.env.example` и описал выше
- [ ] Менял только то, что относится к задаче
- [ ] Миграции БД / удаление файлов — если есть, явно отметил выше
