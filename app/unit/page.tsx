import { UnitMarginPage } from "@/components/opiu/UnitMarginPage";

// UnitMarginPage keeps the shared WbProductImage fallback for every product thumbnail.
export default function UnitPage() {
  return <UnitMarginPage />;
}
