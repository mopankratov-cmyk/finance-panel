"use client";

import { ProductsAnalyticsPage } from "../products/ProductsAnalyticsPage";

/** Склад — фокус на распределении и остатках (общая страница товаров со складским акцентом). */
export function WarehouseAnalyticsPage() {
  return <ProductsAnalyticsPage />;
}
