# Что нужно знать до первой правки

Онбординг (`docs/ONBOARDING.md`) объясняет, как настроить окружение и отправить PR.
Этот документ — про другое: про грабли, которые уже собраны, и про правила, нарушение
которых ломает данные тихо, без ошибки в консоли.

Читается за десять минут. Сэкономит дни.

---

## 1. Как устроен поток данных

```
маркетплейс (WB / Ozon API)
        ↓  app/api/sync/*      — фоновые прогоны по расписанию (vercel.json → crons)
    таблицы в Supabase
        ↓  app/api/<экран>/    — роуты читают базу, считают витрину
      экран (components/)
```

**Правило:** экран не ходит в API маркетплейса напрямую, если этого можно избежать.
Живые запросы упираются в лимиты, отваливаются по таймауту и делают экран непредсказуемым.
Всё, что можно собрать заранее, собирается фоном и кладётся в базу.

---

## 2. Кэш, который кажется кэшем, но им не является

`unstable_cache` (обёртка `loadHourlyDashboard`) **не общий между роутами** и **обнуляется
каждой выкладкой**. Ключ включает текст функции после сборки.

Из-за этого:
- справочник названий товаров то появлялся, то исчезал — жил в кэше сборки;
- остатки FBS «пропадали» после каждого деплоя.

**Правило:** справочные данные, нужные нескольким экранам, живут в таблице, а не в кэше.

**Отдельная ловушка:** если меняешь ФОРМУ данных внутри кэшируемого загрузчика — обязательно
подними версию схемы в ключе (`schema: N`). Иначе экран получит вчерашний снимок без новых
полей, а по коду будет казаться, что фича раскатана. Так уже терялись `stock_fbo` и `stock_fbs`.

---

## 3. WB API: что закрыто и что ломается

| Что | Правда |
|---|---|
| `GET /api/v3/orders/{id}/meta` | **Закрыт**, отвечает 405. Коды маркировки берутся пакетно: `POST /api/marketplace/v3/orders/meta`, до 100 заданий, другой префикс |
| `GET /api/v3/orders?dateFrom=` | Окно **не старше 30 суток**. На 35 — `400 IncorrectParameter`. Это была самоподдерживающаяся ловушка: без успешного прогона нет курсора, без курсора снова просится 35 дней |
| Лимиты | 300 запросов/мин на продавца. **Ответ 4XX считается за 10 запросов** — поток ошибок выжигает лимит вдесятеро и бьёт по соседним экранам |
| Повторы | Лестница повторов внутри одного запроса может занять до 85 секунд. Любой внешний бюджет надо протаскивать ВНУТРЬ запроса, иначе он не соблюдается |
| Фото | Лежит на одном из «баскетов», номер которого выводится из артикула — и иногда выводится неверно. Использовать `WbProductImage` (перебирает варианты), а не одну ссылку. У части карточек фото нет вовсе — рисуется заглушка с артикулом |

**Агентские кабинеты.** WB отдаёт сборочные задания ВСЕГО продавца, а кабинет ограничен
своими товарами. У Оптимы 92% заданий чужие. Фильтровать по товарному контуру обязательно,
иначе бюджет прогона уходит на чужое.

**Календарь площадки — московский.** Окно, посчитанное через `toISOString()`, после 21:00 по Москве
отстаёт на день: сегодняшний день WB в него не попадает. Для дат синков — `lib/sync/moscowDay.ts`.

**Прогрев ходит машиной, а не человеком.** Внутренние запросы несут `Bearer CRON_SECRET`: прокси их
пускает, но роут со своим `requireApiSession` ответит 401, и кэш не наполнится никогда. Для греющихся
GET-роутов есть `requireApiSessionOrMachine`; мутации — только с живой сессией.

---

## 4. Постгрес: что не работает так, как выглядит

- `date::date` по колонке **не sargable** — индекс не используется, запрос идёт последовательным чтением.
- `timestamptz → date` **не IMMUTABLE**, поэтому индекс по выражению создать нельзя (ошибка 42P17).
- Ветки `(p_cabinet is null or cabinet_id = p_cabinet)` требуют `plan_cache_mode = force_custom_plan`,
  иначе планировщик выбирает общий план и запрос деградирует.
- Один PostgREST-upsert целится в **колонки** уникального индекса. Выражение-индексы не подходят.
  Для ключей с NULL — `unique nulls not distinct`.
- **Любая выборка обрезается на тысяче строк.** Молча, без ошибки: приходит ровно 1000 строк, и код
  считает это полным ответом. `limit(50_000)` потолок не отменяет — он ограничивает сверху, а не
  снимает серверный предел. Это касается и RPC: PostgREST режет ответ функции так же, как обычный
  запрос. Читай через `loadAllSupabasePages` (`lib/supabase/loadAllPages.ts`) с обязательным
  `.order(...)` — без сортировки страницы могут перекрываться и терять строки. Помощник бросает
  исключение, если выборка превысила безопасный предел: лучше явная авария, чем тихий обрезок.
- **Обрезку на тысяче строк видно по форме данных.** Последний день с цифрами
  оказывается НЕПОЛНЫМ — меньше соседнего, — а следующие пустые. Если данные
  правда перестали приходить, последний день целый, а следующий пустой. Разница
  в один взгляд, но она отличает «дыра в данных» от «дыра в чтении»: 31.08 по
  этому признаку нашлась обрезка агрегата РНП, а не пропавшие продажи, — до
  того полдня искали не там. Агрегат на строку «день × товар» упирается в
  тысячу уже на 64 товарах за месяц; у кабинета на 18 товаров тот же код
  работает верно, поэтому «у соседа же считается» — не аргумент.
- **Курсор статистики WB — это гонка.** Курсор двигается на максимальный
  `lastChangeDate` ответа, но часть строк площадка публикует ЗАДНИМ ЧИСЛОМ, со
  временем изменения СТАРШЕ уже сохранённого курсора: запрос «строго от
  курсора» такую строку не вернёт никогда. Возвраты при этом идут (событие
  «сейчас»), продажи — нет, и в РНП появляются отрицательные выкупы. Спрашивать
  только с перехлёстом (`statisticsRequestCursor`, двое суток); запись
  идемпотентна, повтор безопасен.
- **У снимка и у сырого слоя разная семантика одного поля.** `wb_rk_journal_daily.spent`
  — уже полная сумма, `wb_advert_nm_campaign_daily.spent` — только измеренная,
  разложенное лежит рядом. Применение одной формулы к обоим давало двойной счёт
  на снятых днях. Прежде чем складывать одинаково названные колонки из двух
  таблиц, проверь, что они означают одно и то же.
- **Апсёрт не умеет забывать.** Строка, пропавшая из ответа площадки (товар кончился, уехал со
  склада, кампания удалена), остаётся в базе с последним известным значением навсегда. Если таблица
  описывает «как сейчас», после ПОЛНОГО обхода обнуляй всё, что этим прогоном не подтвердилось,
  и обязательно сужай обнуление до контура кабинета.

---

## 5. Честность данных — это не стиль, это требование

Самая частая ошибка в этом проекте — показать ноль там, где правда «не знаем».

Примеры настоящих поломок:
- пустая таблица остатков давала «0 на складе» по всем товарам, хотя обход ещё не запускался;
- пустой разбор по кампаниям писал «кампаний не было», хотя реклама шла — просто разбивку не сохраняли;
- 632 пенала помечались «вывести из оборота», хотя они не маркируются вовсе;
- вкладка «Вывести из оборота» показывала ноль всегда: наполнить её было нечем, а ноль читался
  как «нарушений нет». Вкладку убрали — пустое обещание хуже отсутствия раздела;
- синк заказов при полном отбое по лимиту WB писал в журнал «ок»: зелёный журнал при пустых
  экранах не заставляет никого разбираться;
- «Здоровье» писало «Продажи WB — данные синхронизированы» в день, когда продаж не было десять
  суток подряд: проверка смотрела на факт запуска, а не на факт данных. Теперь свежесть источника
  сверяется с соседним (`lib/health/syncFacts.ts`): молчит один при живом соседе — это дыра,
  молчат оба — предупреждение.

**Второе правило:** не рапортуй об успехе, когда данных не появилось. Обрезанный обход, отложенный
по лимиту сбор, неполный опрос складов — это отдельные состояния, и называть их надо словами.
Курсор при этом двигай только до того места, докуда действительно дочитал: сдвинешь дальше —
пропущенное не увидит уже никто.

**Правило:** отличай «нет данных» от «данные есть, и это ноль». Первое — прочерк и объяснение,
второе — цифра. Если объединить, пользователь примет решение по выдуманному факту.

**История остатков (`wb_stocks_history`) не пишет нули.** Снимок раз в четыре часа кладёт только
ненулевые пары «артикул — склад», поэтому у артикула без остатка строк в таблице нет вовсе. Отличить
«остатка не было» от «снимок не делался» можно только по журналу запусков крона: `sync_log`, job
`stocks-history`, статус `ok` (`lib/supplies/stockHistory.ts`, для РНП — SQL-функция
`wb_stock_history_daily`). Снимку неудавшегося запуска верить нельзя — часть строк могла не записаться.
Суточное значение берётся на фиксированный час (`STOCK_SNAPSHOT_HOUR_MSK` = 23:00 по Москве), чтобы дни
сравнивались между собой.

**Баланс не читает текущий остаток и не использует 90-дневную историю.** Для него
все товарные активы фиксируются одним бессрочным снимком строго 1-го числа в
00:01 по Москве (`balance_marketplace_stock_runs/lines`, cron
`balance-monthly-stock`) и показываются четырьмя отдельными кликабельными
группами: фулфилмент, склад WB, склад Ozon, в пути от поставщика. WB/Ozon
оцениваются как `product_costs.cost_rub + product_costs.warehouse_expenses`.
Фулфилмент оценивается по фактической себестоимости складского регистра
**без упаковки**: лежащий там товар ещё не упакован. Товар в пути берётся только
из фактических отгрузок закупок (`shipped/customs/arrived`) и оценивается по цене
заказа × зафиксированный курс, также без упаковки. Если хотя бы у одной позиции
нет себестоимости или не снят хотя бы один обязательный источник, общая сумма
остатков в Балансе не показывается: неполную оценку нельзя выдавать за полный актив.

**Фулфилмент закрывается с лагом, но дата баланса не сдвигается.** Начальный
снимок 1-го числа помечается предварительным. Каждый день в 06:10 МСК cron
повторно сворачивает append-only `stock_moves` строго по
`occurred_at < первое число 00:00 МСК` через `balance_fulfillment_as_of`.
Поэтому документ, внесённый 3-го числа, но датированный последним днём прошлого
месяца, попадёт в нужный баланс; движение с фактической датой после границы —
нет. Признак «предварительно» снимается только когда у всех активных юрлиц
`period_closed_through` закрывает последний день прошлого месяца — произвольного
пятидневного дедлайна нет. Для проверки источников без записи снимка директор
может вызвать `balance-monthly-stock?dryRun=1`: ответ
возвращает количество, стоимость, пропуски себестоимости и контрольные строки.

**Реальный остаток лежит только на «Склад WB» (FBW и FBS).** Склады по городам (Коледино, Казань…)
после пожара пусты, а их строки в отчёте WB — фантом (владелец, 21.09.2026). Сумма по всем складам
завышена: по Retail Family отчёт показал 14 944 шт при 2 370 доступных на витрине. Признак — один:
`isWbWarehouse` (`lib/wb/realStock.ts`), он же в SQL (`WB_WAREHOUSE_SQL_PATTERN`). «В пути к клиенту» и
«от клиента» WB не делит по складам, они считаются по всем строкам артикула. Остаток по всем складам
без фильтра — почти всегда ошибка; новый экран с остатком обязан идти через этот признак.

---

## 6. Витрина маркетплейса — не хранилище оригиналов

WB отдаёт фотографии карточки в двух размерах: `c246x328` для сетки превью и `big`. Оригинала, с
которого их сделали, у WB нет вовсе — восстановить его оттуда невозможно.

При этом `content/v3/media/save` заменяет набор медиафайлов карточки ЦЕЛИКОМ. Значит любой массив
URL, отправленный туда, становится новой галереей. У нас на запись уходил массив, собранный на
экране из миниатюр 246×328, и нажать кнопку могла любая живая сессия: одно нажатие подменило бы
всю галерею почтовыми марками без возможности отката. Не выстрелило только потому, что кнопкой ни
разу не воспользовались.

**Правило:** массив для показа и массив для записи — разные объекты, и второй собирается на
сервере свежим запросом к маркетплейсу, а не приходит с экрана. `PimRow.photos` — только превью,
для всего, что уходит наружу (запись, генерация), есть `photosBig`. Необратимую запись публичного
контента гейтуй ролью директора, а не любой живой сессией.

**Ссылка на фото карточки — адрес позиции, а не файла.** `…/images/big/1.webp` отдаёт то, что
сейчас стоит первым в карточке: после замены обложки тот же адрес отдаёт уже новое фото. Поэтому
всё, что нужно вернуть или показать позже (вариант CTR-теста, исходная обложка), копируется в наш
бакет и хранится там как обычный файл (`lib/ctrtest/pinImage.ts`). Так «Текущее фото» теста 13 ни
разу не вернулось на витрину после первого раунда: автосмена скачивала по его ссылке то, что уже
висело, и заливала обратно.

**CTR-тест ведёт кампанию, но она принадлежит владельцу.** Новый движок (`ctr_tests.engine_version = 2`,
`lib/ctrtest/stepEngine.ts`) сам ставит поисковую кампанию на паузу, когда шаг набрал цель, и
запускает на следующем: пока кампания крутится, счётчики растут всегда, и «устоявшейся» статистики
не бывает. При любом выходе из работы (пауза, конец, отмена) кампания возвращается в состояние до
теста, пока возврат не подтверждён, стоит `campaign_restore_pending` и крон повторяет попытку.
Сбой на любом этапе шаг не завершает и не пропускает: он повторяется на следующем проходе.
Прежние тесты (`engine_version = 1`) идут прежним путём и кампанию не трогают.

---

## 7. Файлы, которые открываются пустыми

XLSX — это XML, а XML 1.0 запрещает управляющие символы. Коды маркировки
Честного Знака содержат разделитель GS (0x1D) между группами: файл собирается,
весит килобайты, содержит все строки — и открывается в Excel ПУСТЫМ, потому что
Excel «чинит» такую книгу, выбрасывая лист. Ошибки не показывает никто.

Управляющие символы записывай так же, как это делает сам Excel: `_xHHHH_` (он
читает их обратно в исходный символ, код не портится), а литеральную
последовательность `_xHHHH_` в тексте экранируй как `_x005F_x...._`. Проверено:
Честный Знак такой файл принимает.

**Правило шире формата:** проверяй РЕЗУЛЬТАТ, а не намерение. Перед отдачей
документа посчитай строки в готовом файле и поищи в нём управляющие символы —
«собрали и отправили» и «получатель увидел данные» это разные события.

---

## 8. Вёрстка таблиц

- У **закреплённой** (`sticky`) ячейки фон обязан быть **непрозрачным**. Под ней проезжают
  другие колонки, и через полупрозрачный фон их цифры читаются поверх — выглядит как наложение
  строк, а измерение геометрии ничего не показывает.
- Ячейка `colSpan` на всю ширину при горизонтальной прокрутке уезжает вместе с таблицей.
  Подпись внутри неё нужно прикреплять к левому краю.

---

## 9. Порядок работы

- **Миграции применяет владелец.** Пиши SQL в `supabase/migrations/`, отдавай текст — накатит он.
- **Выкладка ручная:** `npx vercel --prod --yes --archive=tgz` **только из главного рабочего дерева**.
  Из worktree команда отправит код главного дерева, и новые роуты молча не уедут.
- **Слитый PR ≠ прод.** Гейт вливает сам, выкладку запускает человек.
- **Не допушивай в открытый PR.** Гейт вливает то состояние, что увидел; второй пуш может не успеть.
  Каждая правка — отдельный PR.
- **Проверяй глазами.** Замеры врут: наложение строк было визуальным, а не геометрическим.
  Открой экран, прокрути таблицу, наведи курсор.

---

## 11. Деньги (ДДС): правила и открытые задачи

Модуль `/calendar`, `/payments`, `/accounts`, `/loans` живёт по своим инвариантам — они собраны в
**`docs/DDS-RULES.md`**: один справочник статей (`lib/finance/categories.ts`), один факт ДДС —
одно обязательство (`lib/finance/factLinks.ts`), автозакрытие плана только при точной сумме,
открытие страницы не меняет данные, ноль ≠ «не посчитано». Там же — словарь служебных меток в
`payments.comment`, шаблон постановки задачи исполнителю и список того, что ждёт решения владельца:
график кредита как сущность, модель баланса счёта, приём зарплатной ведомости (готовые ТЗ —
`docs/tz/dds-*.md`).

## 10. Что сейчас открыто

- **Складской флоу** — движение товара от приёмки на фулфилменте до списания КИЗ. Ждём описание
  от владельца, без него код писать бессмысленно.
- **Остатки FBS** — сборщик обходит по кабинету в час, данные накапливаются.
- **Возвраты** — читаются из базы, `srid` заполняется постепенно синком продаж.
