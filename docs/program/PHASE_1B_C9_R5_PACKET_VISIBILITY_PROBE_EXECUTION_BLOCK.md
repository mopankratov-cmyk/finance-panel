# Phase 1B-C9 R5 Packet Visibility Probe Execution Block

Status: `BLOCKED_PACKET_CAPTURE_PERMISSION_REQUIRED`

The owner approved C9 R5 packet visibility probing:

```text
APPROVE_SYNTHETIC_LIMA_PACKET_VISIBILITY_PROBE:p1b-20260722-limapktc9r5:697deae9aeec4518a6edcd6d5986c1ae6dfda1d2cad5b096c8b2c1851d7c5928
```

The local runner validated the contract and approval record, then attempted the
approved probe without privilege escalation. macOS denied packet capture access
before any guest traffic was generated.

## Result

```text
PACKET_CAPTURE_PERMISSION_REQUIRED
```

No `pfctl` command was executed. No host firewall state was changed. No guest
traffic was generated by the failed attempt.

## Manual operator command printed

The contract allows printing a manual admin command, but does not allow Codex to
perform automatic privileged execution.

```text
sudo /usr/sbin/tcpdump -n -tt -q -i utun4 -c 8 'host 192.168.5.15 and tcp and port 443'
```

This command alone only starts the packet capture. A later manual procedure must
coordinate capture with one synthetic guest TCP connect attempt and must return
only sanitized packet direction counts. Do not persist raw packet output.

## Still forbidden

- no `pfctl`;
- no edits to `/etc/pf.conf` or `/etc/pf.anchors/*`;
- no host firewall state changes;
- no raw packet payload persistence;
- no broad packet capture;
- no production profiles;
- no real credentials;
- no gateway/default runtime changes;
- no canary;
- no VM reclaim/delete.

## Next gate

```text
PHASE_1B_C9_R5_R2_MANUAL_PACKET_VISIBILITY_PROCEDURE_OR_FAIL_CLOSED
```

