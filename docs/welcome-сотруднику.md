# Приветственное сообщение новому сотруднику (копипаст в личку)

> Перед отправкой: спроси ник на GitHub и пригласи в репозиторий; ключи пришли через 1Password / Bitwarden Send
> (не открытым текстом); опц. добавь в Vercel как Viewer — чтобы смотрел превью-деплои своих PR.

---

Привет! Берём тебя дорабатывать нашу панель 🚀
У тебя есть Claude Code — этого почти хватит, остальное настроим за 15 минут.

**От тебя сейчас:**
1. Скинь свой ник на GitHub (нет аккаунта — заведи на github.com, бесплатно). Я пришлю приглашение в проект.
2. Поставь 3 инструмента:
   • **Mac:** `brew install git node gh`
   • **Windows (PowerShell):** `winget install Git.Git OpenJS.NodeJS.LTS GitHub.cli`

**Когда придёт приглашение:**
3. Прими его и залогинься: в терминале выполни `gh auth login` → GitHub.com → HTTPS → Login with a web browser.
4. Открой Claude Code в пустой папке и скажи ему дословно:
   > склонируй репозиторий mopankratov-cmyk/finance-panel, открой docs/ONBOARDING.md и проведи меня по настройке по шагам

5. Ключи пришлю через защищённую ссылку (1Password / Bitwarden Send) — положи их в файл `.env.local` в корне проекта, никуда не выкладывай и в git не коммить. (Ещё добавлю тебя в Vercel как Viewer — смотреть превью своих PR.)

**Правила в двух словах:**
- Работаешь в своей ветке, изменения отправляешь через Pull Request. В `main` напрямую не пишем.
- Ключи/пароли в код не вставляем.
- Мелкие безопасные правки вливаются автоматически после проверки; крупные/рискованные — после моего подтверждения. Красный значок проверки на таких PR — это нормально, я их смотрю сам.

Застрянешь — спроси своего Claude Code или меня. Сломать ничего не сможешь, экспериментируй смело 🙂
