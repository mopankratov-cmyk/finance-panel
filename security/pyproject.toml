[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pankster-security-harness"
version = "0.1.0"
description = "Synthetic security harness for PANKSTER Agent Platform Phase 1A."
requires-python = ">=3.11"
