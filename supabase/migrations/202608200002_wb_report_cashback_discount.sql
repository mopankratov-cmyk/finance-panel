-- Компенсация скидки по программе лояльности из финотчёта WB.
-- Без этой колонки строка ОПиУ «Компенсация скидки по программе лояльности»
-- всегда 0: данные у WB есть, но синк их не сохранял.
-- Тип numeric и nullable — как у соседних денежных полей отчёта: NULL значит
-- «в этой строке отчёта поля не было», а не «ноль рублей».
alter table public.wb_report_rows
  add column if not exists cashback_discount numeric;
