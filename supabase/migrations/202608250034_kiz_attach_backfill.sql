-- Разовая привязка уже накопленных кодов. Один запрос, ничего больше.
--
-- Отдельным файлом, потому что между применением функции и первым сбором кодов
-- экран показывал бы ноль у всех юрлиц — а это выглядит как «дел нет», а не как
-- «ещё не разобрано». Дальше привязка вызывается сама в конце каждого сбора.

select public.kiz_attach_legal_entity();
